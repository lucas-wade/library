<% provide(:title, 'Edit Publication') %>

<h1>Edit Publication</h1>


<div class="row edit-form">
  <div class="col-md-6 col-md-offset-3">

    <%= render 'shared/pub_form' %>

    Create a translation
    <!-- check to see if original -->
    <%
       #if t.originals.

    %>
    <%# link_to '<i class="fa fa-language edit-color">Create Translation</i>'.html_safe, new_pub_path(original_id: p.id), class: "btn btn-success" %>


  </div>
</div>
