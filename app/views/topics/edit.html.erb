<% provide(:title, 'Edit topics') %>
<h1>Edit Topics</h1>
<div class="filled-container"><div class="filled-container">

  <%= render 'form' %>

<%# f.fields_for :parents, @topic.parents do |t| %>
    <%# t.collection_select :parents, Topic.all, :id, :name %>
<%# end %>

<% @topic.parents.each do |t| %>
    Parents <br>
      <ul class="list-inline2">
        <li>
      <%= t.name %> id= <%= t.id %>
          </li>
        <li>
    <% current_topic.parent_families.find_by(parent_id: t).parent_id %>
    <%= form_for(current_topic.parent_families.find_by(parent_id: t),
                 html: { method: :delete }) do |f| %>
    <%= f.submit "Remove Parent", class: "btn btn-primary" %>
    <% end %>
        </li>
        </ul>
    <br>
<% end %>

  NOT WORKING Add a parent NOT WORKING
  <%= form_for(current_topic.parent_families.create) do |f| %>
      <div><%= f.number_field :parent_id %></div>
      <%= f.submit "Parent id #", class: "btn btn-primary" %>
  <% end %>



</div></div>