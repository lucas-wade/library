<!DOCTYPE html>
<html>

<head>
  <title><%=full_title(yield(:title)) %></title>
  <%= render 'layouts/scripts' %>
</head>


<body>

<%= render 'layouts/header' %>

<div class="container-fluid <%=  "main-container" if edit_mode != 1 %>">
  <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>


  <div class="jimbo">
    <%= yield %>
  </div>

  <%# render 'layouts/footer' %>

  <%# debug(params) if Rails.env.development? %>

  <% if current_admin == TRUE && edit_mode == 1 %>
      <%# render 'admin/console' %>
  <% end %>

<%= render 'layouts/footer' %>
</div> <!-- /container -->
<div class="green-strip">
  You can help us reach everyone with the knowledge of health and sustainability.
  Please ￼share￼ this site widely and then Join Us! in making possible its completion.
</div>


</body> <!-- /body -->

</html>
